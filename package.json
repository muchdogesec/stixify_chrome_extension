{
  "name": "stixify-chrome-extension",
  "version": "1.0.6",
  "type": "module",
  "scripts": {
    "build": "npm run icons && vite build && node scripts/copy-files.js",
    "icons": "node scripts/generate-icons.js"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.4",
    "sharp": "^0.33.5",
    "vite": "^8.0.5"
  },
  "dependencies": {
    "vue": "^3.4.21"
  }
}
