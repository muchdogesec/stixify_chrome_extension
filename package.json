{
  "name": "stixify-chrome-extension",
  "version": "1.0.2",
  "type": "module",
  "scripts": {
    "build": "npm run sync-version && npm run icons && vite build && node scripts/copy-files.js",
    "icons": "node scripts/generate-icons.js",
    "sync-version": "node scripts/sync-version.js"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.4",
    "sharp": "^0.33.5",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "vue": "^3.4.21"
  }
}
